<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.test.mybatis.IUserDAO">
	
	<select id="userList" resultType="com.test.mybatis.UserDTO">
		SELECT 'ADVICE' AS USERID, '리아트리스' AS NICKNAME, 'img/badge150pixel_0025_맛잘알.png' AS URL, '서울' AS INTERSUBID1, '관악구' AS ADDRGUID1, '서울' AS INTERSUBID2, '마포구' AS ADDRGUID2
     , '게임' AS INTERSUBID1, '수영' AS INTERSUBID2, 4 AS REVIEWGRADE
		FROM DUAL
	</select>
	
	<select id="reviewList" resultType="com.test.mybatis.TestDTO">
		SELECT E.BADGELOGID AS BADGELOGID, E.NICKNAME AS NICKNAME, P.TITLE AS TITLE, P.CONTENTS AS CONTENTS, P.MEETDATE AS MEETDATE, S.ADDRSINAME AS ADDRSINAME, G.ADDRGUNAME AS ADDRGUNAME
		FROM USER_ESSENTIAL E JOIN POST P
		ON E.USERID =P.USERID
		JOIN ADDR_GU G
		ON P.ADDRGUID= G.ADDRGUID
		JOIN ADDR_SI S
		ON G.ADDRSIID = S.ADDRSIID
		WHERE E.USERID = 'admin001'
	</select>
	
	
</mapper>
